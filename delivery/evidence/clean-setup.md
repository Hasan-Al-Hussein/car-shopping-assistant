# Clean extracted setup — actual scoped evidence

A fresh internal candidate was installed and exercised on 25 September 2026. Dependency installation, the real backend entrypoint, a five-turn live conversation and new-session recall, connected inventory browsing, local enquiry saving and physical CSV export are supported by saved evidence. This is not an unqualified whole-project or final-release PASS.

## Package and installation

Case: `q11-c-a65227aa38ce`. The internal candidate contained 335 selected files, verified after extraction and installation. Its SHA-256 is `8734519237a1f0b4dd65075f3293c1bc80c524848a0cc188812b23d67a88347d`; source-manifest digest `2a5577ce10d5ad1132e7eb52044dc69e769394a159ebbcf1915f10a62b63679d`. Its status remains `UNVERIFIED_CANDIDATE_NOT_A_RELEASE`.

| Recorded step | Actual outcome |
| --- | --- |
| Plan / candidate / extraction | Plan exit 2 with only missing release-readiness; candidate and extraction exit 0. |
| Backend installation | Fresh extraction-local environment; `uv sync --frozen --python <preinstalled Python 3.13.3>`, exit 0, 50 packages. |
| Frontend installation | Fresh extraction-local dependencies; `npm ci --no-audit --no-fund`, exit 0, 312 packages. |
| Bootstrap and first activation | Initial bootstrap failed; approved same-case mapped-directory preparation followed by successful bootstrap and `ACTIVATION_RECORDED`. |
| Full application | Extracted root `main.py`, real clock and Vite development server reached readiness. |
| Packaged HTTP demo | Exit0; 19 HTTP requests and five available Gemini turns, one adapter attempt each. |
| Connected browser | At `http://127.0.0.1:5173/__app`: 100 inventory cars, saved local enquiry, draft and viewing review page. Confirmation was not clicked. |

Approved download caches were warm; neither an empty-cache nor offline installation is claimed. Normal install scripts were enabled. The environment excludes system site packages. Installed Uvicorn was 0.53.0 and Vite 8.3.0. npm's unknown `jobs` configuration warning did not prevent its successful exit.

## Corrections and original failures

The first bootstrap emitted `BOOTSTRAP_FAILED`, exit 1, before a Store existed. Source and saved path observations support a missing mapped physical runtime directory as the cause; the generic output did not expose an original exception code. Provisioning that already approved directory resolved the host orchestration omission without replacing the case, package, Store generation or operation. Ordinary README setup uses equal logical/physical roots and its auto-initialization is source-supported; it was not separately executed as an ordinary unmapped-host installation.

The first backend owner also retained `FAILED/HEALTH_SOURCE_MISMATCH`: its receipt check expected an obsolete Store-relative path. A separate owner copy corrected that expectation; product source was unchanged.

The original README dev URL opened a static design proof. The actual connected route is `/__app`, and the root README's one-line correction was applied after source-freeze release. The demo HTTP origin remains `http://127.0.0.1:5173`, without a path. The tested candidate retains its original README bytes; a final package must bind the corrected documentation separately.

## Actual conversation and CSV

The HTTP demonstration retained Ford Explorer 2013/source 1 across mileage and warranty follow-ups and correctly reported both facts as unknown. It saved the synthetic preferences “quiet cabin” and “space for a folding bicycle” in one session, then recalled both in a distinct session with the same saved revision/provenance. The client does not independently attest provider mode; five matching host diagnostic events provide the separate live-provider join. Journal SHA-256: `34490299c9845ddf796e55e594234647a49f4dbf6a09525a285a40dd09a58d09`.

The later connected browser made no recorded assistant/provider request. It saved a synthetic cash budget of AED 10,000–20,000 and quiet-cabin/folding-bicycle needs, with both optional contacts declined. Independently inspected exported bytes have one row, 24 columns, 1,058 bytes, revision/projection 1, and SHA-256 `8e7a76e5b961794d6f5e59698927595b97ba03774a8f3ead9eb76fb344d14b36`. The publication manifest and saved browser lead identity agree. This supplements the earlier included `qualified-lead.csv`; it does not replace that file or its explanatory note. The new row has no linked booking. A review page or saved draft is not booking confirmation.

## Closure and scope

Both browser invocations were released after their fixed work cutoffs. Their supervisors remain `FAILED/LIFETIME_EXHAUSTED`; the connected browser remains `PARTIAL_LATE_RELEASE`. Application/launcher and Vite shutdown nevertheless exited0 without forced kill, with settled application lifespan and released maintenance admission. Final saved observations show 22 original-run and 11 connected-run recorded identities absent, both ports clear and no active claim/latch. Continuous maintenance admission through late browser teardown and an exhaustive unknown-descendant census are not claimed.

The original closeout SHA-256 is `a6b58f4a4da974bd6fca1c4274d5925c3f9d09892810e4c5c2d5c594b6b6b841`; the connected closeout is `769101590113caabafe3f83864d2aa5d2045a5d37bb0ecbb9fcb58e20a386f10`. The original demo's false source-match summary is retained: its 354 ordered rows match, but one identical duplicate produces 353 unique paths and defeats that summary's uniqueness predicate. A provisioning observation's standalone companion suffixes are also retained as an explicitly limited metadata record.

These are reviewed, synthetic demonstrations on the identified package. Original failures and the missed original delivery deadline are preserved. No full Q11, 70-card, arbitrary-prompt reliability or final archive acceptance is assigned here.

